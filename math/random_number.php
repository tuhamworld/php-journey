<?php
// Generating a random Number

echo(rand());

// To have more control over random number, you can specify the range

echo(rand(10,100)); // Implies from between 10 to 100 will the random number be generated


?>